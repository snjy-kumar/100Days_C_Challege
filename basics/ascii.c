// Online C compiler to run C program online
#include <stdio.h>

int main() {
    // Write C code here
    //find the ascii value of which character is entered by user.
    char ch;
    printf("Enter the character you would like to see their ascii value: ");
    scanf("%c", &ch);
    printf("The ascii value of character %c and their value is %d", ch , ch);

    return 0;
}